package 상근날드;

import java.util.Scanner;

// 상근날드에 3가지 햄버거와 2가지 음료를 판매합니다.
// 3가지 햄버거 중에 가장 싼 메뉴의 가격을 선택하고, 음료 중 가장 싼 메뉴의 가격을 선택
// 세트 메뉴는 50원 할인 됨.
// 1000 1500 2000
public class SetMenu {
    public static void main(String[] args) {
        /*


        Scanner sc = new Scanner(System.in);
        int[] price = new int[5];
        int tmp = 0;

        System.out.print("햄버거의 가격을 입력하세요 : ");
        for (int i = 0; i < price.length; i++) {
            price[i] = sc.nextInt();
        }
        int minBurger = price[0];
        int minDrink = price[3];

        for(int i = 0; i < price.length; i++) {
            if((i < 3) && (minBurger > price[i])) minBurger = price[i];
            if((i > 2) && (minDrink > price[i])) minDrink = price[i];
        }
        System.out.println("가격은 " + ((minBurger + minDrink)-50) + " 입니다.");


         */
    }
}